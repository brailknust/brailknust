.next
node_modules
.env*
!.env.example
!.env.test.example
*.log
*.tsbuildinfo
coverage
playwright-report
test-results

# Local OCR benchmarking tools and their Python virtual environment.
# (Previously only excluded via the machine-local .git/info/exclude, which
# tools other than git itself — e.g. EAS Build's archiver — don't read.)
tmp/
